import { Nav } from "~/components/nav";

export function Join() {
    return (
        <main className="relative min-h-screen bg-[#050505] font-mono text-neutral-400">
            <Nav />
        </main>
    );
}
